# Copy to .env.local for local `vercel dev` testing (that file is gitignored).
# In production, set this in Vercel: Project Settings → Environment Variables.
# Never put a real value in this file — it IS committed to git.
DATA_API_KEY=
